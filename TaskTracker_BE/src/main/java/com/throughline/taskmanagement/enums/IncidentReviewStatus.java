package com.throughline.taskmanagement.enums;

/** Risk Review / Compliance Review columns from the Excel register. */
public enum IncidentReviewStatus {
    PENDING,
    COMPLETED,
    NOT_REQUIRED
}
